package Rough;

public class rough {
/*	public void getData(WebElement pageNumberElement) throws IOException {

		List<WebElement> ele = or.getElements(tableRowXpath);

		for (WebElement e : ele) {
			List<WebElement> row = or.getChilds(e, "./td");
			data = new ArrayList<String>();
			System.out.println("Page Number :" + or.getText(pageNumberElement));
			for (WebElement rowElement : row) {
				String tempData = or.getText(rowElement);
				data.add(tempData);
				System.out.print(" " + tempData);

			}
			
			json.writeJson(data);
			System.out.println();
		}

	}

	public void allPage() throws IOException {
		pages = or.getElements(pageNumber_Button);

		for (int i = 0; i < pageSize; i++) {
			WebElement page = pages.get(i);
			or.click(page);
			or.waitforSecond();
			pages = or.getElements(pageNumber_Button);
			page = pages.get(i);
			getData(page);

		}
	}

	public void signlePage(int pagenumber) throws IOException {
		pages = or.getElements(pageNumber_Button);

		WebElement page = pages.get(pagenumber - 1);
		or.click(page);
		or.waitforSecond();
		pages = or.getElements(pageNumber_Button);
		page = pages.get(pagenumber - 1);
		getData(page);

	}

	public void betweenRage(int from, int to) throws IOException {
		for (int i = from; i <= to; i++) {
			WebElement page = pages.get(i);
			or.click(page);
			or.waitforSecond();
			pages = or.getElements(pageNumber_Button);
			page = pages.get(i);
			getData(page);

		}
	
*/
}
